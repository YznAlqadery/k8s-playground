apiVersion: v1
kind: ConfigMap
metadata:
  name: synergychat-web-configmap
data:
  WEB_PORT: "8080"
  API_URL: "http://synchatapi.internal" # The URL of the API service that the web application will communicate with