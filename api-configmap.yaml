apiVersion: v1
kind: ConfigMap
metadata:
  name: synergychat-api-config
data:
  API_PORT: "8080"
  API_DB_FILEPATH: "/persist/db.json"
  CRAWLER_BASE_URL: "http://crawler-service.crawler.svc.cluster.local" # The URL of the crawler service that the API will communicate with